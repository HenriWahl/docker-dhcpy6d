
name: build-release-latest
on:
  push:
    tags-ignore: 'v*'
    branches: '**'

jobs:
  build-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: pwd
      - run: ls -l
